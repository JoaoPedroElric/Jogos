#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

// Renomeando a struct.
typedef struct atributoplayer Atributoplayer;

// Função para inicializar os atributos do player.
void Inicializar_Atributos(Atributoplayer *player);
